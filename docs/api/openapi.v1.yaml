openapi: 3.1.0
info:
  title: CRM Silmer API
  version: 1.0.0
  description: Contratos versionados de identidade e domínio do CRM Silmer.
servers:
  - url: /api/v1
paths:
  /bootstrap/identity:
    post:
      operationId: bootstrapIdentity
      summary: Cria o único Admin inicial
      parameters:
        - $ref: '#/components/parameters/BootstrapToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BootstrapRequest'
      responses:
        '201':
          description: Admin criado uma única vez
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BootstrapResponse'
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
  /users:
    get:
      operationId: listUsers
      summary: Lista todas as contas, inclusive as desativadas
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Contas do CRM
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [users]
                properties:
                  users:
                    type: array
                    items: { $ref: '#/components/schemas/User' }
        '401': { $ref: '#/components/responses/InvalidCredentials' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      operationId: createUser
      summary: Cria uma conta de Vendedor por Admin autenticado
      description: >-
        A senha em claro nunca retorna no corpo da resposta; o bloco de
        entrega é montado no cliente a partir do que foi digitado.
      security:
        - sessionCookie: []
          csrfHeader: []
          idempotencyKey: []
      parameters:
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserRequest'
      responses:
        '201':
          description: Conta criada
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [user]
                properties:
                  user: { $ref: '#/components/schemas/User' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
  /users/{userId}:
    delete:
      operationId: deleteUser
      summary: Exclui permanentemente uma conta sem histórico operacional
      description: >-
        Contas com COMMERCIAL_ADMIN nunca podem ser excluídas. Se a conta tiver
        histórico operacional, a API responde 409 USER_HAS_HISTORY; nesse caso,
        a desativação é o caminho disponível.
      security:
        - sessionCookie: []
          csrfHeader: []
          idempotencyKey: []
      parameters:
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [reason]
              properties:
                reason: { type: string, minLength: 1 }
      responses:
        '200':
          description: Conta excluída ou replay idempotente
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [deleted]
                properties:
                  deleted: { type: boolean, const: true }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
    patch:
      operationId: updateUser
      summary: Altera nome, e-mail ou senha de uma conta
      description: >-
        Todo campo é opcional e pelo menos um deve vir. Um campo ausente
        permanece inalterado. Trocar a senha não revoga sessões abertas.
      security:
        - sessionCookie: []
          csrfHeader: []
          idempotencyKey: []
      parameters:
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserRequest'
      responses:
        '200':
          description: Conta atualizada
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [user]
                properties:
                  user: { $ref: '#/components/schemas/User' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
  /users/{userId}/{state}:
    post:
      operationId: setUserDisabled
      summary: Desativa ou reativa uma conta
      security:
        - sessionCookie: []
          csrfHeader: []
          idempotencyKey: []
      parameters:
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/UserId'
        - name: state
          in: path
          required: true
          schema: { type: string, enum: [disable, enable] }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [reason]
              properties:
                reason: { type: string, minLength: 1 }
      responses:
        '200':
          description: Situação da conta alterada
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [user]
                properties:
                  user: { $ref: '#/components/schemas/User' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
  /sessions:
    post:
      operationId: createSession
      summary: Autentica sem enumerar contas e cria cookies separados
      parameters:
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Sessão criada; nenhum token integra o corpo
          headers:
            Set-Cookie:
              description: crm_session HttpOnly e crm_csrf legível pelo cliente
              schema: { type: string }
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionView'
        '401': { $ref: '#/components/responses/InvalidCredentials' }
        '429': { $ref: '#/components/responses/Throttled' }
  /sessions/current:
    get:
      operationId: getCurrentSession
      summary: Retorna o usuário da sessão e toca a expiração ociosa
      security:
        - sessionCookie: []
      responses:
        '200':
          description: Sessão atual
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SessionView' }
        '401': { $ref: '#/components/responses/InvalidCredentials' }
    delete:
      operationId: deleteCurrentSession
      summary: Revoga a sessão atual
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - $ref: '#/components/parameters/Origin'
      responses:
        '204': { description: Sessão revogada e cookies expirados }
        '403': { $ref: '#/components/responses/Forbidden' }
  /capabilities/{change}:
    post:
      operationId: changeCapability
      summary: Concede ou revoga capacidade e audita a mudança
      security:
        - sessionCookie: []
          csrfHeader: []
          idempotencyKey: []
      parameters:
        - $ref: '#/components/parameters/Origin'
        - name: change
          in: path
          required: true
          schema: { type: string, enum: [grant, revoke] }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CapabilityRequest'
      responses:
        '200':
          description: Mudança confirmada
          content:
            application/json:
              schema:
                type: object
                required: [changed]
                properties:
                  changed: { type: boolean, const: true }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
  /inbox/conversations:
    get:
      operationId: listInboxConversations
      summary: Lista o backlog operacional de conversas
      description: >-
        Projeção paginada e minimizada. Cursores são assinados e vinculados aos
        filtros para impedir troca silenciosa de escopo.
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: '#/components/parameters/HumanReadOrigin'
        - {
            name: state,
            in: query,
            schema: { $ref: '#/components/schemas/ConversationState' },
          }
        - {
            name: channel,
            in: query,
            schema: { $ref: '#/components/schemas/Channel' },
          }
        - {
            name: automationState,
            in: query,
            schema: { type: string, enum: [assistant, human] },
          }
        - {
            name: assignedUserId,
            in: query,
            schema: { type: string, minLength: 1, maxLength: 128 },
          }
        - {
            name: archived,
            in: query,
            schema: { type: boolean, default: false },
            description: Quando true, retorna somente conversas arquivadas.
          }
        - { name: cursor, in: query, schema: { type: string, maxLength: 1024 } }
        - {
            name: limit,
            in: query,
            schema: { type: integer, minimum: 1, maximum: 100, default: 50 },
          }
      responses:
        '200':
          description: Página de conversas, sem envelopes cifrados ou payload bruto
          headers:
            Cache-Control:
              { schema: { type: string, const: 'private, no-cache' } }
            Vary: { schema: { type: string, const: 'Origin, Cookie' } }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/InboxPage' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /inbox/conversations/{conversationId}:
    get:
      operationId: getInboxConversation
      summary: Retorna uma conversa e seu histórico persistido
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: '#/components/parameters/ConversationId'
        - $ref: '#/components/parameters/HumanReadOrigin'
      responses:
        '200':
          description: Conversa, mensagens e sugestão pendente minimizadas
          content:
            application/json:
              schema: { $ref: '#/components/schemas/InboxConversationDetail' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /inbox/handoffs:
    get:
      operationId: listOpenHandoffs
      summary: Lista handoffs humanos pendentes
      description: >-
        Projeção paginada e minimizada para a fila de atendimento. O resumo é
        decifrado somente depois da autorização do operador autenticado.
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: '#/components/parameters/HumanReadOrigin'
        - { name: cursor, in: query, schema: { type: string, maxLength: 1024 } }
        - {
            name: limit,
            in: query,
            schema: { type: integer, minimum: 1, maximum: 100, default: 50 },
          }
      responses:
        '200':
          description: Página de handoffs pendentes, sem envelopes cifrados
          content:
            application/json:
              schema: { $ref: '#/components/schemas/HandoffPage' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /contacts:
    get:
      operationId: listContacts
      summary: Lista contatos canônicos e suas identidades por canal
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: '#/components/parameters/HumanReadOrigin'
        - { name: cursor, in: query, schema: { type: string, maxLength: 1024 } }
        - {
            name: limit,
            in: query,
            schema: { type: integer, minimum: 1, maximum: 100, default: 50 },
          }
      responses:
        '200':
          description: Página de contatos, sem envelopes cifrados
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ContactPage' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /contacts/{contactId}:
    get:
      operationId: getContact
      summary: Retorna contato, identidades, conversas e negócios associados
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: '#/components/parameters/ContactId'
        - $ref: '#/components/parameters/HumanReadOrigin'
      responses:
        '200':
          description: Histórico operacional minimizado do contato
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ContactDetail' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /kanban:
    get:
      operationId: getKanban
      summary: Projeta as cinco colunas do Kanban a partir de Negócios ativos
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: '#/components/parameters/HumanReadOrigin'
        - {
            name: assignedUserId,
            in: query,
            schema: { type: string, minLength: 1 },
          }
        - { name: hasOverdueTask, in: query, schema: { type: boolean } }
        - {
            name: limit,
            in: query,
            schema: { type: integer, minimum: 1, maximum: 100, default: 20 },
          }
      responses:
        '200':
          description: Colunas com paginação independente; Backlog não integra o Kanban
          headers:
            Cache-Control:
              { schema: { type: string, const: 'private, no-cache' } }
            Vary: { schema: { type: string, const: 'Origin, Cookie' } }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KanbanBoard' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /kanban/cards:
    get:
      operationId: getKanbanCards
      summary: Pagina uma etapa do Kanban por updatedAt e Deal id
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: '#/components/parameters/HumanReadOrigin'
        - {
            name: stage,
            in: query,
            required: true,
            schema: { $ref: '#/components/schemas/DealStage' },
          }
        - { name: cursor, in: query, schema: { type: string, maxLength: 1024 } }
        - {
            name: assignedUserId,
            in: query,
            schema: { type: string, minLength: 1 },
          }
        - { name: hasOverdueTask, in: query, schema: { type: boolean } }
        - {
            name: limit,
            in: query,
            schema: { type: integer, minimum: 1, maximum: 100, default: 20 },
          }
      responses:
        '200':
          description: Página da coluna solicitada
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KanbanColumn' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /deals/{dealId}:
    get:
      operationId: getDealDetail
      summary: Retorna detalhe operacional pseudônimo do Negócio
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/HumanReadOrigin'
        - { name: If-None-Match, in: header, schema: { type: string } }
      responses:
        '200':
          description: Negócio, qualificação, tarefas, gates, histórico e handoffs sem texto cifrado
          headers:
            ETag: { schema: { type: string } }
            Cache-Control:
              { schema: { type: string, const: 'private, no-cache' } }
            Vary: { schema: { type: string, const: 'Origin, Cookie' } }
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DealDetail' }
        '304': { description: Representação composta não mudou }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /events:
    get:
      operationId: streamKanbanEvents
      summary: Reproduz invalidações do Kanban em ordem total durável
      security: [{ sessionCookie: [] }]
      parameters:
        - $ref: '#/components/parameters/HumanReadOrigin'
        - {
            name: topic,
            in: query,
            required: true,
            schema: { type: string, const: kanban },
          }
        - { name: after, in: query, schema: { type: integer, minimum: 0 } }
        - {
            name: Last-Event-ID,
            in: header,
            schema: { type: integer, minimum: 0 },
          }
      responses:
        '200':
          description: >-
            text/event-stream. Emite kanban.card.changed, stream.reset e
            heartbeats; nenhum payload livre de domínio é transportado.
          content:
            text/event-stream: { schema: { type: string } }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '429':
          description: Limite de aberturas por origem ou de streams simultâneos excedido
          headers:
            Retry-After: { schema: { type: integer, minimum: 1 } }
  /conversations/{conversationId}/convert:
    post:
      operationId: convertConversation
      summary: Vincula o Contato e cria exatamente um Negócio em Produto
      description: >-
        Fecha a conversa como convertida_em_lead na mesma transação da promoção
        do Contato, criação do Negócio, idempotência e auditoria. Aceita o ator
        técnico do n8n ou uma sessão humana de Vendedor.
      security:
        - automationBasic: []
          idempotencyKey: []
        - sessionCookie: []
          csrfHeader: []
          idempotencyKey: []
      parameters:
        - name: conversationId
          in: path
          required: true
          schema: { type: string, minLength: 1 }
        - $ref: '#/components/parameters/OriginOptional'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConversationConversionRequest'
      responses:
        '201':
          description: Conversão criada ou resposta original reproduzida
          headers:
            Location:
              description: URL canônica do Negócio criado
              schema: { type: string }
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConversationConversionResponse'
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '401': { $ref: '#/components/responses/InvalidCredentials' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '503': { $ref: '#/components/responses/Unavailable' }
  /deals/{dealId}/transitions:
    post:
      operationId: transitionDeal
      summary: Avança ou retorna exatamente uma etapa do Negócio
      description: >-
        O destino é sempre derivado da etapa atual. O avanço persiste a
        avaliação do gate, o histórico e o evento canônico na mesma transação.
        O executor técnico deve informar a conversa ativa e seu automationEpoch.
      security:
        - automationBasic: []
          idempotencyKey: []
        - sessionCookie: []
          csrfHeader: []
          idempotencyKey: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/OriginOptional'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DealTransitionRequest' }
      responses:
        '200':
          description: Transição aplicada ou resposta original reproduzida
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DealCommandResponse' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '401': { $ref: '#/components/responses/InvalidCredentials' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '503': { $ref: '#/components/responses/Unavailable' }
  /deals/{dealId}/lose:
    post:
      operationId: loseDeal
      summary: Marca o Negócio como perdido com motivo cifrado
      description: Exclusivo para Vendedor autenticado.
      security:
        - sessionCookie: []
          csrfHeader: []
          idempotencyKey: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DealLossRequest' }
      responses:
        '200':
          description: Perda registrada ou resposta original reproduzida
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DealCommandResponse' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '503': { $ref: '#/components/responses/Unavailable' }
  /deals/{dealId}/fields:
    patch:
      operationId: patchDealFields
      summary: Atualiza campos oficiais da Ficha do Negócio
      description: >-
        Patch idempotente de seções fechadas. Incrementa a versão raiz uma vez
        e retorna atomicamente à primeira etapa anterior incompleta. O executor
        técnico deve provar conversa ativa e automationEpoch na mesma transação.
      security:
        - automationBasic: []
          idempotencyKey: []
        - sessionCookie: []
          csrfHeader: []
          idempotencyKey: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/OriginOptional'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DealFieldsPatchRequest' }
      responses:
        '200':
          description: Campos persistidos ou resposta original reproduzida
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DealFieldsPatchResponse' }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '401': { $ref: '#/components/responses/InvalidCredentials' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/InvalidRequest' }
        '503': { $ref: '#/components/responses/Unavailable' }
  /deals/{dealId}/assign:
    post:
      operationId: assignDeal
      summary: Atribui um Negócio a um Vendedor ativo
      security: [{ sessionCookie: [], csrfHeader: [], idempotencyKey: [] }]
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DealAssignmentRequest' }
      responses:
        '200': { description: Atribuição persistida ou replay idempotente }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/InvalidRequest' }
  /deals/{dealId}/tasks:
    post:
      operationId: createFollowUpTask
      summary: Cria tarefa humana de follow-up com texto cifrado
      security: [{ sessionCookie: [], csrfHeader: [], idempotencyKey: [] }]
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TaskCreateRequest' }
      responses:
        '201': { description: Tarefa criada ou replay idempotente }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/InvalidRequest' }
  /tasks/{taskId}/{action}:
    post:
      operationId: transitionFollowUpTask
      summary: Inicia, conclui ou cancela uma tarefa de follow-up
      description: Tarefas vinculadas a handoff não podem ser canceladas isoladamente.
      security: [{ sessionCookie: [], csrfHeader: [], idempotencyKey: [] }]
      parameters:
        - name: taskId
          in: path
          required: true
          schema: { type: string, minLength: 1 }
        - name: action
          in: path
          required: true
          schema: { type: string, enum: [start, complete, cancel] }
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TaskActionRequest' }
      responses:
        '200': { description: Tarefa atualizada ou replay idempotente }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
  /deals/{dealId}/handoffs:
    post:
      operationId: createHumanHandoff
      summary: Cria handoff humano e sua única tarefa atomicamente
      description: AUTOMATION_EXECUTOR deve informar contexto n8n e fence da conversa.
      security:
        - automationBasic: []
          idempotencyKey: []
        - sessionCookie: []
          csrfHeader: []
          idempotencyKey: []
      parameters:
        - $ref: '#/components/parameters/DealId'
        - $ref: '#/components/parameters/OriginOptional'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/HandoffCreateRequest' }
      responses:
        '201': { description: Handoff e tarefa criados ou replay idempotente }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '401': { $ref: '#/components/responses/InvalidCredentials' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/InvalidRequest' }
  /handoffs/{handoffId}/{action}:
    post:
      operationId: transitionHumanHandoff
      summary: Aceita, transfere ou resolve um handoff humano
      description: Transferência preserva dueAt; nenhuma ação reativa a automação.
      security: [{ sessionCookie: [], csrfHeader: [], idempotencyKey: [] }]
      parameters:
        - name: handoffId
          in: path
          required: true
          schema: { type: string, minLength: 1 }
        - name: action
          in: path
          required: true
          schema: { type: string, enum: [accept, transfer, resolve] }
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/HandoffActionRequest' }
      responses:
        '200':
          { description: Handoff e tarefa atualizados ou replay idempotente }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
  /integrations/n8n/messages/inbound:
    post:
      operationId: receiveN8nInboundMessage
      summary: Persiste uma mensagem recebida pelo n8n e devolve o contexto oficial
      security: [{ automationBasic: [], idempotencyKey: [] }]
      parameters:
        - $ref: '#/components/parameters/CorrelationId'
        - $ref: '#/components/parameters/WorkflowKey'
        - $ref: '#/components/parameters/WorkflowVersion'
        - $ref: '#/components/parameters/ExecutionId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/N8nInboundRequest' }
      responses:
        '200':
          description: Evento aceito ou replay idempotente
          content:
            application/json:
              schema: { $ref: '#/components/schemas/N8nInboundResponse' }
        '400': { $ref: '#/components/responses/N8nProblem' }
        '401': { $ref: '#/components/responses/N8nProblem' }
        '403': { $ref: '#/components/responses/N8nProblem' }
        '409': { $ref: '#/components/responses/N8nProblem' }
        '422': { $ref: '#/components/responses/N8nProblem' }
        '503': { $ref: '#/components/responses/N8nProblem' }
  /integrations/n8n/conversations/{conversationId}/attachments:
    post:
      operationId: storeN8nAttachment
      summary: Valida em streaming, quarentena e publica uma mídia transitória segura
      security: [{ automationBasic: [], idempotencyKey: [] }]
      parameters:
        - $ref: '#/components/parameters/ConversationId'
        - $ref: '#/components/parameters/CorrelationId'
        - $ref: '#/components/parameters/WorkflowKey'
        - $ref: '#/components/parameters/WorkflowVersion'
        - $ref: '#/components/parameters/ExecutionId'
        - $ref: '#/components/parameters/ContentSha256'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema: { $ref: '#/components/schemas/N8nAttachmentRequest' }
      responses:
        '200': { description: Replay idempotente do anexo já publicado }
        '201': { description: Anexo verificado e publicado }
        '400': { $ref: '#/components/responses/N8nProblem' }
        '401': { $ref: '#/components/responses/N8nProblem' }
        '409': { $ref: '#/components/responses/N8nProblem' }
        '413': { $ref: '#/components/responses/N8nProblem' }
        '415': { $ref: '#/components/responses/N8nProblem' }
        '422': { $ref: '#/components/responses/N8nProblem' }
        '503': { $ref: '#/components/responses/N8nProblem' }
  /integrations/n8n/events:
    post:
      operationId: recordN8nEvent
      summary: Aplica evento n8n com idempotência, fencing e transação atômica
      security: [{ automationBasic: [], idempotencyKey: [] }]
      parameters:
        - $ref: '#/components/parameters/CorrelationId'
        - $ref: '#/components/parameters/WorkflowKey'
        - $ref: '#/components/parameters/WorkflowVersion'
        - $ref: '#/components/parameters/ExecutionId'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/N8nEventRequest' }
      responses:
        '200':
          description: Evento aplicado, negado pelo fence ou reproduzido idempotentemente
          content:
            application/json:
              schema: { $ref: '#/components/schemas/N8nEventResponse' }
        '400': { $ref: '#/components/responses/N8nProblem' }
        '401': { $ref: '#/components/responses/N8nProblem' }
        '403': { $ref: '#/components/responses/N8nProblem' }
        '409': { $ref: '#/components/responses/N8nProblem' }
        '422': { $ref: '#/components/responses/N8nProblem' }
        '503': { $ref: '#/components/responses/N8nProblem' }
  /conversations/{conversationId}/messages:
    post:
      operationId: sendHumanConversationMessage
      summary: Persiste a intenção humana e agenda o comando idempotente ao n8n
      security: [{ sessionCookie: [], csrfHeader: [], idempotencyKey: [] }]
      parameters:
        - $ref: '#/components/parameters/ConversationId'
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/HumanMessageCommand' }
      responses:
        '202': { description: Estado oficial e outbox gravados atomicamente }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
  /conversations/{conversationId}/{command}:
    post:
      operationId: changeConversationAutomationMode
      summary: Executa takeover, retorno à IA ou encerramento humano
      security: [{ sessionCookie: [], csrfHeader: [], idempotencyKey: [] }]
      parameters:
        - $ref: '#/components/parameters/ConversationId'
        - name: command
          in: path
          required: true
          schema: { type: string, enum: [takeover, return-to-ai, close] }
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/HumanConversationCommand' }
      responses:
        '202': { description: Estado oficial, auditoria e outbox gravados }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
  /conversations/{conversationId}/archive:
    post:
      operationId: archiveConversation
      summary: Retira uma conversa ativa da Caixa de Entrada sem encerrá-la
      security: [{ sessionCookie: [], csrfHeader: [], idempotencyKey: [] }]
      parameters:
        - $ref: '#/components/parameters/ConversationId'
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/HumanConversationCommand' }
      responses:
        '202': { description: Conversa arquivada, com auditoria e atualização em tempo real }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
  /conversations/{conversationId}/unarchive:
    post:
      operationId: unarchiveConversation
      summary: Restaura uma conversa arquivada à Caixa de Entrada
      security: [{ sessionCookie: [], csrfHeader: [], idempotencyKey: [] }]
      parameters:
        - $ref: '#/components/parameters/ConversationId'
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/HumanConversationCommand' }
      responses:
        '202': { description: Conversa desarquivada, com auditoria e atualização em tempo real }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
  /handoffs/{handoffId}/claim:
    post:
      operationId: claimHumanHandoff
      summary: Reivindica atomicamente um handoff compatível ainda sem responsável
      security: [{ sessionCookie: [], csrfHeader: [], idempotencyKey: [] }]
      parameters:
        - name: handoffId
          in: path
          required: true
          schema: { type: string, minLength: 1 }
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/HandoffClaimRequest' }
      responses:
        '200': { description: Handoff atribuído à pessoa autenticada }
        '400': { $ref: '#/components/responses/InvalidRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
components:
  securitySchemes:
    automationBasic:
      type: http
      scheme: basic
      description: Credencial exclusiva e rotacionável do AUTOMATION_EXECUTOR.
    sessionCookie:
      type: apiKey
      in: cookie
      name: crm_session
      description: Cookie opaco HttpOnly, Secure e SameSite=Lax.
    csrfHeader:
      type: apiKey
      in: header
      name: X-CSRF-Token
      description: Deve corresponder ao cookie Secure crm_csrf.
    idempotencyKey:
      type: apiKey
      in: header
      name: Idempotency-Key
  parameters:
    Origin:
      name: Origin
      in: header
      required: true
      schema: { type: string, format: uri }
    OriginOptional:
      name: Origin
      in: header
      required: false
      description: Obrigatório para autenticação humana; não usado pelo ator técnico.
      schema: { type: string, format: uri }
    HumanReadOrigin:
      name: Origin
      in: header
      required: false
      description: >-
        Se presente, deve pertencer à allowlist. Se ausente, o navegador deve
        enviar Sec-Fetch-Site same-origin ou none.
      schema: { type: string, format: uri }
    BootstrapToken:
      name: X-Bootstrap-Token
      in: header
      required: true
      schema: { type: string, minLength: 32 }
    UserId:
      name: userId
      in: path
      required: true
      schema: { type: string, minLength: 1 }
    CorrelationId:
      name: X-Correlation-Id
      in: header
      required: true
      schema: { type: string, minLength: 1, maxLength: 128 }
    WorkflowKey:
      name: X-Silmer-Workflow-Key
      in: header
      required: true
      schema: { type: string, minLength: 1, maxLength: 128 }
    WorkflowVersion:
      name: X-Silmer-Workflow-Version
      in: header
      required: true
      schema: { type: string, minLength: 1, maxLength: 64 }
    ExecutionId:
      name: X-Silmer-Execution-Id
      in: header
      required: true
      schema: { type: string, minLength: 1, maxLength: 128 }
    ContentSha256:
      name: X-Silmer-Content-SHA256
      in: header
      required: true
      schema: { type: string, pattern: '^[a-f0-9]{64}$' }
    ConversationId:
      name: conversationId
      in: path
      required: true
      schema: { type: string, minLength: 1 }
    ContactId:
      name: contactId
      in: path
      required: true
      schema: { type: string, minLength: 1, maxLength: 128 }
    DealId:
      name: dealId
      in: path
      required: true
      schema: { type: string, minLength: 1 }
  schemas:
    N8nMode:
      type: string
      enum: [ai_active, handoff_pending, human_active, closed]
    N8nInboundRequest:
      type: object
      additionalProperties: false
      required:
        [
          schema_version,
          event_id,
          occurred_at,
          channel,
          contact,
          message,
          metadata,
        ]
      properties:
        schema_version: { type: string, const: '1.0' }
        event_id: { type: string, minLength: 1, maxLength: 512 }
        occurred_at: { type: string, format: date-time }
        channel: { type: string, const: whatsapp }
        contact:
          type: object
          anyOf:
            - required: [external_id]
            - required: [wa_id]
          properties:
            external_id: { type: string, minLength: 1, maxLength: 512 }
            wa_id: { type: string, minLength: 1, maxLength: 512 }
            name: { type: string, maxLength: 512 }
        message:
          type: object
          additionalProperties: true
          required: [external_id, type]
          properties:
            external_id: { type: string, minLength: 1, maxLength: 512 }
            type:
              type: string
              enum: [text, image, audio, video, document, button, interactive]
            text: { type: string, maxLength: 65536 }
        metadata:
          type: object
          additionalProperties: true
          anyOf:
            - required: [provider_account_id]
            - required: [phone_number_id]
    N8nInboundResponse:
      type: object
      additionalProperties: true
      required:
        [
          accepted,
          duplicate,
          conversation_id,
          source_revision,
          automation_epoch,
          mode,
          recent_messages,
          briefing,
        ]
      properties:
        accepted: { type: boolean, const: true }
        duplicate: { type: boolean }
        conversation_id: { type: string }
        source_revision: { type: integer, minimum: 1 }
        conversation_version: { type: integer, minimum: 1 }
        contact_id: { type: string }
        automation_epoch: { type: integer, minimum: 0 }
        mode: { $ref: '#/components/schemas/N8nMode' }
        recent_messages: { type: array, items: { type: object } }
        briefing_version: { type: integer, minimum: 0 }
        briefing:
          type: object
          additionalProperties: true
          description: Snapshot criptografado e consolidado da qualificação.
    N8nAttachmentRequest:
      type: object
      additionalProperties: false
      required: [schema_version, external_id, file]
      properties:
        schema_version: { type: string, const: '1.0' }
        external_id: { type: string, minLength: 1, maxLength: 512 }
        filename: { type: string, minLength: 1, maxLength: 255 }
        mime_type: { type: string, minLength: 1, maxLength: 255 }
        file: { type: string, format: binary }
    N8nEventRequest:
      type: object
      additionalProperties: false
      required: [schema_version, event_id, event_type, occurred_at]
      properties:
        schema_version: { type: string, const: '1.0' }
        event_id: { type: string, minLength: 1, maxLength: 512 }
        event_type:
          type: string
          enum:
            [
              message.send.requested,
              message.sent,
              message.delivered,
              message.read,
              message.failed,
              message.send.unknown,
              handoff.requested,
              workflow.failed,
            ]
        occurred_at: { type: string, format: date-time }
        conversation_id: { type: [string, 'null'] }
        automation_epoch: { type: integer, minimum: 0 }
        source_revision: { type: integer, minimum: 1 }
        command_id: { type: string, maxLength: 512 }
        external_message_id: { type: string, maxLength: 512 }
        message: { type: object, additionalProperties: true }
        briefing_patch:
          type: object
          additionalProperties: false
          properties:
            artwork_locations: {}
            artwork_status: {}
            artwork_technique: {}
            briefing_status: {}
            city_or_postal_code: {}
            colors: {}
            customer_name: {}
            customizations: {}
            delivery_address: {}
            delivery_mode: {}
            fabrics: {}
            needed_by: {}
            next_required_field: {}
            notes: {}
            numbers: {}
            order_name: {}
            pickup_location: {}
            product_model: {}
            product_type: {}
            purchase_profile: {}
            purpose: {}
            quantity: {}
            segment: {}
            sizes: {}
            sponsors: {}
        handoff: { type: object, additionalProperties: true }
        failure: { type: object, additionalProperties: true }
    N8nEventResponse:
      type: object
      additionalProperties: true
      required: [accepted]
      properties:
        accepted: { type: boolean }
        duplicate: { type: boolean }
        send_authorized: { type: boolean }
        reason: { type: string }
    N8nProblem:
      type: object
      additionalProperties: false
      required:
        [type, title, status, detail, instance, accepted, error, request_id]
      properties:
        type: { type: string, format: uri-reference }
        title: { type: string }
        status: { type: integer, minimum: 400, maximum: 599 }
        detail: { type: string }
        instance: { type: string }
        accepted: { type: boolean, const: false }
        error:
          type: object
          additionalProperties: false
          required: [code]
          properties: { code: { type: string } }
        request_id: { type: string }
    HumanMessageCommand:
      type: object
      additionalProperties: false
      required: [content, expectedVersion, messageType, reason]
      properties:
        content: { type: object, additionalProperties: true }
        expectedVersion: { type: integer, minimum: 1 }
        messageType:
          { type: string, enum: [text, image, audio, video, document] }
        reason: { type: string, minLength: 1, maxLength: 512 }
    HumanConversationCommand:
      type: object
      additionalProperties: false
      required: [expectedVersion, reason]
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        reason: { type: string, minLength: 1, maxLength: 512 }
    HandoffClaimRequest:
      type: object
      additionalProperties: false
      required: [expectedHandoffVersion, reasonCode]
      properties:
        expectedHandoffVersion: { type: integer, minimum: 1 }
        expectedConversationVersion: { type: integer, minimum: 1 }
        reasonCode: { type: string, minLength: 1, maxLength: 512 }
    Channel:
      type: string
      enum: [instagram, whatsapp]
    ConversationState:
      type: string
      enum:
        [
          nova,
          em_analise,
          em_atendimento,
          requer_atencao,
          convertida_em_lead,
          sem_lead,
        ]
    ContactIdentityView:
      type: object
      additionalProperties: false
      required: [id, channel, kind, externalId, displayHandle, phoneStatus]
      properties:
        id: { type: string }
        channel: { $ref: '#/components/schemas/Channel' }
        kind: { type: string }
        externalId: { type: string }
        displayHandle: { type: [string, 'null'] }
        phoneStatus: { type: [string, 'null'] }
    ContactView:
      type: object
      additionalProperties: false
      required:
        [
          id,
          label,
          provisional,
          version,
          identities,
          activeDealCount,
          createdAt,
          updatedAt,
          archivedAt,
          latestActivityAt,
        ]
      properties:
        id: { type: string }
        label: { type: string }
        provisional: { type: boolean }
        version: { type: integer, minimum: 1 }
        identities:
          type: array
          items: { $ref: '#/components/schemas/ContactIdentityView' }
        activeDealCount: { type: integer, minimum: 0 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        latestActivityAt: { type: string, format: date-time }
    InboxMessageView:
      type: object
      additionalProperties: false
      required:
        [id, direction, type, preview, status, deliveryStatus, occurredAt]
      properties:
        id: { type: string }
        direction: { type: string, enum: [inbound, outbound] }
        type: { type: string }
        preview: { type: string }
        status: { type: string }
        deliveryStatus: { type: [string, 'null'] }
        occurredAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        authorKind: { type: [string, 'null'] }
        authorId: { type: [string, 'null'] }
    InboxConversationView:
      type: object
      additionalProperties: false
      required:
        [
          id,
          state,
          automationState,
          automationEpoch,
          version,
          channel,
          contact,
          lastMessage,
          requiresAttention,
          openedAt,
          updatedAt,
        ]
      properties:
        id: { type: string }
        state: { $ref: '#/components/schemas/ConversationState' }
        automationState: { type: string, enum: [assistant, human] }
        automationEpoch: { type: integer, minimum: 0 }
        version: { type: integer, minimum: 1 }
        channel: { $ref: '#/components/schemas/Channel' }
        contact:
          type: object
          additionalProperties: false
          required: [id, label, externalId, displayHandle]
          properties:
            id: { type: string }
            label: { type: string }
            externalId: { type: string }
            displayHandle: { type: [string, 'null'] }
        assignedUser:
          {
            oneOf:
              [
                { $ref: '#/components/schemas/OperationalUserRef' },
                { type: 'null' },
              ],
          }
        deal: { type: [object, 'null'] }
        handoff: { type: [object, 'null'] }
        lastMessage:
          {
            oneOf:
              [
                { $ref: '#/components/schemas/InboxMessageView' },
                { type: 'null' },
              ],
          }
        requiresAttention: { type: boolean }
        openedAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        archivedAt: { type: [string, 'null'], format: date-time }
        terminalAt: { type: [string, 'null'], format: date-time }
    InboxPage:
      type: object
      additionalProperties: false
      required: [items, nextCursor, totalCount]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/InboxConversationView' }
        nextCursor: { type: [string, 'null'] }
        totalCount: { type: integer, minimum: 0 }
    InboxConversationDetail:
      type: object
      additionalProperties: false
      required: [conversation, messages, suggestion]
      properties:
        conversation: { $ref: '#/components/schemas/InboxConversationView' }
        messages:
          type: array
          items: { $ref: '#/components/schemas/InboxMessageView' }
        suggestion: { type: [object, 'null'] }
    HandoffView:
      type: object
      additionalProperties: false
      required:
        [
          id,
          conversationId,
          conversationVersion,
          contact,
          status,
          version,
          reasonCode,
          summary,
          targetRole,
          dueAt,
          slaMinutes,
          createdAt,
          updatedAt,
        ]
      properties:
        id: { type: string }
        conversationId: { type: string }
        conversationVersion: { type: integer, minimum: 1 }
        contact:
          type: object
          additionalProperties: false
          required: [id, label, externalId]
          properties:
            id: { type: string }
            label: { type: string }
            externalId: { type: string }
        status: { type: string, const: pending }
        version: { type: integer, minimum: 1 }
        reasonCode: { type: string }
        summary: { type: string }
        targetRole: { type: string }
        dueAt: { type: string, format: date-time }
        slaMinutes: { type: integer, minimum: 1 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        dealId: { type: [string, 'null'] }
        automationEpoch: { type: integer, minimum: 0 }
    HandoffPage:
      type: object
      additionalProperties: false
      required: [items, nextCursor, totalCount]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/HandoffView' }
        nextCursor: { type: [string, 'null'] }
        totalCount: { type: integer, minimum: 0 }
    ContactPage:
      type: object
      additionalProperties: false
      required: [items, nextCursor, totalCount]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/ContactView' }
        nextCursor: { type: [string, 'null'] }
        totalCount: { type: integer, minimum: 0 }
    ContactDetail:
      type: object
      additionalProperties: false
      required: [contact, identities, conversations, deals, activeDealCount]
      properties:
        contact: { $ref: '#/components/schemas/ContactView' }
        identities:
          type: array
          items: { $ref: '#/components/schemas/ContactIdentityView' }
        conversations:
          type: array
          items: { type: object }
        deals:
          type: array
          items: { type: object }
        activeDealCount: { type: integer, minimum: 0 }
    DealStage:
      type: string
      enum: [produto, especificacao, estampa, logistica, fechamento]
    OperationalUserRef:
      type: object
      additionalProperties: false
      required: [id, functionName]
      properties:
        id: { type: string }
        functionName: { type: string, enum: [Vendedor] }
    TaskView:
      type: object
      additionalProperties: false
      required: [id, type, status, assignedUserId, dueAt, version]
      properties:
        id: { type: string }
        type: { type: string, enum: [follow_up, human_handoff] }
        status: { type: string, enum: [pending, in_progress] }
        assignedUserId: { type: string }
        dueAt: { type: string, format: date-time }
        version: { type: integer, minimum: 1 }
    KanbanCard:
      type: object
      additionalProperties: false
      required:
        [
          id,
          stage,
          status,
          version,
          updatedAt,
          stageEnteredAt,
          totalQuantity,
          assignedUser,
          nextTask,
          nextPendingField,
        ]
      properties:
        id:
          {
            type: string,
            description: O próprio Deal id; Card não é entidade persistida.,
          }
        stage: { $ref: '#/components/schemas/DealStage' }
        status: { type: string, const: active }
        version: { type: integer, minimum: 1 }
        updatedAt: { type: string, format: date-time }
        stageEnteredAt: { type: string, format: date-time }
        totalQuantity: { type: integer, minimum: 0 }
        assignedUser:
          oneOf:
            [
              { type: 'null' },
              { $ref: '#/components/schemas/OperationalUserRef' },
            ]
        nextTask:
          oneOf: [{ type: 'null' }, { $ref: '#/components/schemas/TaskView' }]
        nextPendingField:
          oneOf: [{ type: 'null' }, { type: string }]
    KanbanColumn:
      type: object
      additionalProperties: false
      required: [stage, label, count, cards, nextCursor]
      properties:
        stage: { $ref: '#/components/schemas/DealStage' }
        label:
          {
            type: string,
            enum: [Produto, Especificação, Estampa, Logística, Fechamento],
          }
        count: { type: integer, minimum: 0 }
        cards:
          { type: array, items: { $ref: '#/components/schemas/KanbanCard' } }
        nextCursor:
          oneOf: [{ type: 'null' }, { type: string, maxLength: 1024 }]
    KanbanBoard:
      type: object
      additionalProperties: false
      required: [columns]
      properties:
        columns:
          type: array
          minItems: 5
          maxItems: 5
          items: { $ref: '#/components/schemas/KanbanColumn' }
    HandoffView:
      type: object
      additionalProperties: false
      required:
        [
          id,
          conversationId,
          assignedUserId,
          targetRole,
          status,
          version,
          reasonCode,
          dueAt,
          slaMinutes,
          slaPolicyVersion,
          createdAt,
          updatedAt,
          resolvedAt,
          dealVersion,
          conversationVersion,
          taskId,
          taskVersion,
        ]
      properties:
        id: { type: string }
        conversationId: { type: string }
        assignedUserId: { type: [string, 'null'] }
        targetRole: { type: string, enum: [Vendedor] }
        status: { type: string, enum: [pending, accepted, resolved] }
        version: { type: integer, minimum: 1 }
        reasonCode: { type: string }
        dueAt: { type: string, format: date-time }
        slaMinutes: { type: integer, minimum: 5, maximum: 10080 }
        slaPolicyVersion: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        resolvedAt:
          { oneOf: [{ type: 'null' }, { type: string, format: date-time }] }
        dealVersion: { type: integer, minimum: 1 }
        conversationVersion: { type: integer, minimum: 1 }
        taskId: { type: string }
        taskVersion: { type: integer, minimum: 1 }
    DealDetail:
      type: object
      additionalProperties: false
      required: [deal, qualification, tasks, nextTask, gates, history, handoffs]
      properties:
        deal:
          type: object
          additionalProperties: true
          required:
            [id, contactRef, stage, status, version, updatedAt, assignedUser]
        qualification:
          type: object
          description: Dados estruturados e indicadores de presença; texto cifrado nunca é exposto.
          additionalProperties: true
        tasks:
          type: array
          items: { $ref: '#/components/schemas/TaskView' }
        nextTask:
          oneOf: [{ type: 'null' }, { $ref: '#/components/schemas/TaskView' }]
        gates:
          { type: array, items: { type: object, additionalProperties: true } }
        history:
          { type: array, items: { type: object, additionalProperties: true } }
        handoffs:
          { type: array, items: { $ref: '#/components/schemas/HandoffView' } }
    User:
      type: object
      additionalProperties: false
      required: [id, name, email, functionName, capabilities, canDelete]
      properties:
        id: { type: string }
        name: { type: string, minLength: 1, maxLength: 120 }
        email: { type: string, format: email }
        functionName: { type: string, enum: [Vendedor] }
        createdAt: { type: [string, 'null'], format: date-time }
        disabledAt: { type: [string, 'null'], format: date-time }
        capabilities:
          type: array
          uniqueItems: true
          items: { $ref: '#/components/schemas/Capability' }
        canDelete:
          type: boolean
          description: Indica se a conta não possui histórico operacional e pode ser excluída.
    Capability:
      type: string
      enum: [COMMERCIAL_ADMIN]
    LoginRequest:
      type: object
      additionalProperties: false
      required: [email, password]
      properties:
        email: { type: string, format: email }
        password: { type: string, minLength: 1 }
    SessionView:
      type: object
      additionalProperties: false
      required: [user]
      properties:
        user: { $ref: '#/components/schemas/User' }
    BootstrapRequest:
      type: object
      additionalProperties: false
      required: [email, name, password, reason]
      properties:
        email: { type: string, format: email }
        name: { type: string, minLength: 1, maxLength: 120 }
        functionName: { type: string, enum: [Vendedor] }
        password: { type: string, minLength: 1 }
        reason: { type: string, minLength: 1 }
    BootstrapResponse:
      type: object
      required: [user]
      properties:
        user: { $ref: '#/components/schemas/User' }
    CreateUserRequest:
      type: object
      additionalProperties: false
      required: [email, name, password, reason]
      properties:
        email: { type: string, format: email }
        name: { type: string, minLength: 1, maxLength: 120 }
        password: { type: string, minLength: 1 }
        reason: { type: string, minLength: 1 }
    UpdateUserRequest:
      type: object
      additionalProperties: false
      required: [reason]
      minProperties: 2
      properties:
        email: { type: string, format: email }
        name: { type: string, minLength: 1, maxLength: 120 }
        password: { type: string, minLength: 1 }
        reason: { type: string, minLength: 1 }
    CapabilityRequest:
      type: object
      additionalProperties: false
      required: [targetId, capability, reason]
      properties:
        targetId: { type: string, minLength: 1 }
        capability: { $ref: '#/components/schemas/Capability' }
        reason: { type: string, minLength: 1 }
    ConversationConversionRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion, reason]
      properties:
        automationEpoch:
          type: integer
          minimum: 0
          description: >-
            Obrigatório para AUTOMATION_EXECUTOR e rejeitado se estiver obsoleto
            ou se uma pessoa tiver assumido a conversa. Omitido por humanos.
        expectedVersion: { type: integer, minimum: 1 }
        reason: { type: string, minLength: 1 }
    ConversationConversionResponse:
      type: object
      additionalProperties: false
      required: [contact, conversation, deal]
      properties:
        contact:
          type: object
          additionalProperties: false
          required: [id, provisional, version]
          properties:
            id: { type: string }
            provisional: { type: boolean, const: false }
            version: { type: integer, minimum: 1 }
        conversation:
          type: object
          required: [id, state, terminalAt, version]
          properties:
            id: { type: string }
            state: { type: string, const: convertida_em_lead }
            terminalAt: { type: string, format: date-time }
            version: { type: integer, minimum: 2 }
        deal:
          type: object
          additionalProperties: false
          required: [id, contactId, sourceConversationId, stage, version]
          properties:
            id: { type: string }
            contactId: { type: string }
            sourceConversationId: { type: string }
            stage: { type: string, const: produto }
            version: { type: integer, const: 1 }
    DealTransitionRequest:
      type: object
      additionalProperties: false
      required: [direction, expectedVersion, reason]
      properties:
        direction: { type: string, enum: [advance, retreat] }
        expectedVersion: { type: integer, minimum: 1 }
        reason: { type: string, minLength: 1 }
        conversationId:
          type: string
          minLength: 1
          description: Obrigatório e exclusivo para AUTOMATION_EXECUTOR.
        automationEpoch:
          type: integer
          minimum: 0
          description: Obrigatório e exclusivo para AUTOMATION_EXECUTOR.
    DealLossRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion, reason]
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        reason:
          type: string
          minLength: 1
          description: Motivo sensível cifrado antes da persistência.
    DealFieldsPatchRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion, fields, reasonCode]
      properties:
        expectedVersion: { type: integer, minimum: 1 }
        reasonCode:
          type: string
          enum:
            [
              customer_update,
              correction,
              qualification_review,
              automation_extraction,
            ]
        reasonDetail:
          type: string
          minLength: 1
          maxLength: 2048
          description: Detalhe sensível cifrado; eventos e auditoria recebem somente código e hash.
        conversationId: { type: string, minLength: 1 }
        automationEpoch: { type: integer, minimum: 0 }
        fields:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            order:
              type: object
              additionalProperties: false
              minProperties: 1
              properties:
                customer: { type: string, minLength: 1 }
                name: { type: string, minLength: 1 }
                commercialIntent: { type: boolean, const: true }
            items:
              type: array
              minItems: 1
              items: { $ref: '#/components/schemas/DealItemFields' }
            artwork: { $ref: '#/components/schemas/DealArtworkFields' }
            logistics: { $ref: '#/components/schemas/DealLogisticsFields' }
            observations:
              type: array
              items: { type: string, minLength: 1 }
            assessments:
              type: array
              items: { $ref: '#/components/schemas/FieldAssessment' }
    DealItemFields:
      type: object
      additionalProperties: false
      required: [id]
      minProperties: 2
      properties:
        id: { type: string, minLength: 1 }
        operation: { type: string, enum: [upsert, remove] }
        catalogVersionId: { type: string, minLength: 1 }
        productCode: { type: string, minLength: 1 }
        modelCode: { type: string, minLength: 1 }
        estimatedQuantity: { type: integer, minimum: 1 }
        fabrics:
          type: array
          items: { type: string, minLength: 1 }
        colors:
          type: object
          additionalProperties: false
          properties:
            front: { type: string, minLength: 1 }
            back: { type: string, minLength: 1 }
            rightSleeve: { type: string, minLength: 1 }
            leftSleeve: { type: string, minLength: 1 }
            collarTrim: { type: string, minLength: 1 }
            sleeveTrim: { type: string, minLength: 1 }
        grade:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [size, quantity]
            properties:
              size: { type: string, minLength: 1 }
              quantity: { type: integer, minimum: 1 }
              duplicateSizeReason: { type: string, minLength: 1 }
    DealArtworkFields:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        status:
          {
            type: string,
            enum:
              [ready, customer_will_send, silmer_will_create, not_applicable],
          }
        catalogVersionId: { type: string, minLength: 1 }
        responsibility: { type: string, minLength: 1 }
        techniqueCode: { type: string, minLength: 1 }
        locations:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [id, name]
            properties:
              id: { type: string, minLength: 1 }
              name: { type: string, minLength: 1 }
        files:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [attachmentId, itemId, locationId]
            properties:
              attachmentId:
                type: object
                additionalProperties: false
                required: [messageId, transientMediaId]
                properties:
                  messageId: { type: string, minLength: 1, maxLength: 64 }
                  transientMediaId:
                    { type: string, minLength: 1, maxLength: 64 }
              itemId: { type: string, minLength: 1 }
              locationId: { type: string, minLength: 1 }
        colors:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [locationId, values]
            properties:
              locationId: { type: string, minLength: 1 }
              values:
                type: array
                minItems: 1
                items: { type: string, minLength: 1 }
    DealLogisticsFields:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        desiredDate: { type: string, format: date }
        purpose: { type: string, minLength: 1 }
        purchaseProfile: { type: string, minLength: 1 }
        mode: { type: string, enum: [delivery, pickup] }
        city: { type: string, minLength: 1 }
        address: { type: string, minLength: 1 }
        pickupLocation: { type: string, minLength: 1 }
    FieldAssessment:
      type: object
      additionalProperties: false
      required: [field, status]
      properties:
        field: { type: string, minLength: 1 }
        status:
          {
            type: string,
            enum: [preenchido, nao_aplicavel, pendente, divergente],
          }
        reason: { type: string, minLength: 1 }
    DealFieldsPatchResponse:
      type: object
      additionalProperties: false
      required: [deal, readiness, totalQuantity]
      properties:
        deal:
          { $ref: '#/components/schemas/DealCommandResponse/properties/deal' }
        readiness:
          type: object
          additionalProperties:
            type: object
            additionalProperties: false
            required: [complete, blockers]
            properties:
              complete: { type: boolean }
              blockers: { type: array, items: { type: string } }
              nextPendingField: { type: string }
        totalQuantity:
          type: integer
          minimum: 0
          description: Soma derivada das quantidades estimadas de todos os itens.
    DealCommandResponse:
      type: object
      additionalProperties: false
      required: [deal]
      properties:
        deal:
          type: object
          additionalProperties: true
          required: [id, stage, status, version]
          properties:
            id: { type: string }
            stage:
              type: string
              enum: [produto, especificacao, estampa, logistica, fechamento]
            status: { type: string, enum: [active, lost] }
            version: { type: integer, minimum: 2 }
        gate:
          oneOf:
            - type: 'null'
            - type: object
              additionalProperties: false
              required: [dealId, fromStage, version, blockers, evaluatedAt]
              properties:
                dealId: { type: string }
                fromStage:
                  type: string
                  enum: [produto, especificacao, estampa, logistica]
                version: { type: integer, minimum: 1 }
                blockers:
                  type: array
                  maxItems: 0
                evaluatedAt: { type: string, format: date-time }
    DealAssignmentRequest:
      type: object
      additionalProperties: false
      required: [assignedUserId, expectedDealVersion, reasonCode]
      properties:
        assignedUserId: { type: string, minLength: 1 }
        expectedDealVersion: { type: integer, minimum: 1 }
        reasonCode: { type: string, enum: [manual_assignment] }
    TaskCreateRequest:
      type: object
      additionalProperties: false
      required:
        [assignedUserId, dueAt, expectedDealVersion, reasonCode, text, type]
      properties:
        assignedUserId: { type: string, minLength: 1 }
        dueAt: { type: string, format: date-time }
        expectedDealVersion: { type: integer, minimum: 1 }
        reasonCode: { type: string, enum: [manual_follow_up] }
        text: { type: string, minLength: 1, maxLength: 4096 }
        type: { type: string, const: follow_up }
    TaskActionRequest:
      type: object
      additionalProperties: false
      required: [expectedTaskVersion, reasonCode]
      properties:
        expectedTaskVersion: { type: integer, minimum: 1 }
        reasonCode:
          { type: string, enum: [task_started, task_completed, task_cancelled] }
    HandoffCreateRequest:
      type: object
      additionalProperties: false
      required:
        [
          assignedUserId,
          conversationId,
          expectedConversationVersion,
          expectedDealVersion,
          reasonCode,
          summary,
        ]
      properties:
        assignedUserId: { type: string, minLength: 1 }
        conversationId: { type: string, minLength: 1 }
        expectedConversationVersion: { type: integer, minimum: 1 }
        expectedDealVersion: { type: integer, minimum: 1 }
        reasonCode:
          type: string
          enum:
            [
              customer_requested_human,
              price_before_quote,
              unresolved_blocker,
              low_confidence,
            ]
        summary: { type: string, minLength: 1, maxLength: 8192 }
        automationEpoch: { type: integer, minimum: 0 }
        automationContext:
          type: object
          additionalProperties: false
          required: [executionId, workflowKey, workflowVersion]
          properties:
            executionId: { type: string, minLength: 1 }
            workflowKey: { type: string, minLength: 1 }
            workflowVersion: { type: string, minLength: 1 }
    HandoffActionRequest:
      type: object
      additionalProperties: false
      required:
        [
          expectedConversationVersion,
          expectedDealVersion,
          expectedHandoffVersion,
          expectedTaskVersion,
          reasonCode,
        ]
      properties:
        assignedUserId:
          type: string
          minLength: 1
          description: Obrigatório somente em transfer.
        expectedConversationVersion: { type: integer, minimum: 1 }
        expectedDealVersion: { type: integer, minimum: 1 }
        expectedHandoffVersion: { type: integer, minimum: 1 }
        expectedTaskVersion: { type: integer, minimum: 1 }
        reasonCode:
          {
            type: string,
            enum: [handoff_accepted, manual_transfer, handoff_resolved],
          }
    Error:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error:
          type: object
          additionalProperties: false
          required: [code]
          properties:
            code: { type: string }
  responses:
    N8nProblem:
      description: Erro compatível em application/problem+json sem eco de PII
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/N8nProblem' }
    InvalidRequest:
      description: Entrada inválida sem eco de dados recebidos
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    InvalidCredentials:
      description: Credenciais inválidas, sem indicar se a conta existe
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    Forbidden:
      description: Origem, CSRF, sessão ou autorização inválidos
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    NotFound:
      description: Pessoa alvo da mudança de capacidade não encontrada
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    Conflict:
      description: Chave idempotente divergente ou bootstrap já consumido
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    Throttled:
      description: Bloqueio progressivo ativo
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    Unavailable:
      description: Dependência de autenticação ou persistência indisponível
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
